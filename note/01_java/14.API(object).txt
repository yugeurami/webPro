
[hashcode]
.hashcode();
개체의 hashcode값을 출력
hashcode는 주소는 아니지만 주소값을 변환한 것


[clone]
객체 자신을 복제해서 새로운 객체를 생성하는 메소드
Cloneable 인터페이스를 구현한 클래스의 인스턴스만 복제 가능
Object 클래스에 정의된 clone()은 인스턴스 변수의 값만 복제
인스턴스 변수가 참조형일 때 참조하는 객체도 복제되게 오버라이드해야함

﻿